    <h2><%= link_to portfolio_item.title, portfolio_show_path(portfolio_item) %></h2>
    
    <p><%= portfolio_item.subtitle%></p>
    
    <p><%= portfolio_item.body%></p>
    
    <%#= image_tag portfolio_item.thumb_image if  !portfolio_item.thumb_im %> <%#  statement is better because it renders the code fully allowing for that lack of image exception. Basically : show this image unless it doesnt exisit%>
    <%= image_tag portfolio_item.thumb_image unless portfolio_item.thumb_image.nil? %> <%# the if statement is better because it renders the code fully allowing for that lack of image exception. Basically : show this image unless it doesnt exisit%>
    <%# ^^this was in his code? %>

    <%= link_to 'Edit', edit_portfolio_path(portfolio_item)  if logged_in?(:site_admin)%>
    
    <%= link_to 'Delete this item', portfolio_path(portfolio_item), method: :delete, data: { confirm: 'Are you sure?' }  if logged_in?(:site_admin)%>
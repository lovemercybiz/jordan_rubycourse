<h1>React Portfolio Items</h1>

<%= link_to "Create New Item", new_portfolio_url  if logged_in?(:site_admin)%> <%#this says path because the 'rake routes' in terminal are prefixes to '_path' or '_url'; '_path' for  relative path rather than full url%>
<%# '_url' is for the full url, subdomain  options here ;  '_path' is for relative bath to the already present url%>


<% @react_portfolio_items.each do |portfolio_item| %>
    <h2><%= link_to portfolio_item.title, portfolio_show_path(portfolio_item) %></h2>
    <p><%= portfolio_item.subtitle%></p>
    <p><%= portfolio_item.body%></p>
    <%= image_tag portfolio_item.thumb_image if  !portfolio_item.thumb_image.nil? %> <%# the if statement is better because it renders the code fully allowing for that lack of image exception. Basically : show this image unless it doesnt exisit%>
    <%= link_to 'Edit', edit_portfolio_path(portfolio_item)  if logged_in?(:site_admin)%>
    <%= link_to 'Delete this item', portfolio_path(portfolio_item), method: :delete, data: { confirm: 'Are you sure?' }  if logged_in?(:site_admin)%>
<% end %>